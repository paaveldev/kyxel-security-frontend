# Kyxel Security — Frontend

This repository contains the Astro + Tailwind frontend for Kyxel Security. It is a static-first marketing site with a dashboard UI skeleton prepared for backend integration.

Quick start:

1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
