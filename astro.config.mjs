import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://paaveldev.github.io/kyxel-security-frontend/',
  base: '/kyxel-security-frontend/',
  integrations: [],
});
